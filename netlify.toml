[build]
  command = "npm run build && node scripts/inject-auth-callback-router.cjs"
  publish = "dist"
  functions = "netlify/functions"

[functions."daily-report"]
  schedule = "@hourly"

[functions."provider-sync"]
  schedule = "@hourly"
